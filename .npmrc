# @vercel/speed-insights declares optional peers (e.g. @sveltejs/kit) that pull vite@8;
# this project uses vitest with vite@7. npm's strict peer resolution then fails with ERESOLVE.
legacy-peer-deps=true
